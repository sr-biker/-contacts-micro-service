apiVersion: v1
kind: Service
metadata:
  name: {{ include "contacts-micro-service.name" . }}
  labels:
    {{- include "contacts-micro-service.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "contacts-micro-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      # Not environment-dependent (never overridden in values-local.yaml/values-prod.yaml),
      # so it's inlined here rather than parameterized via values.
      port: 8080
      targetPort: http
